# SQLite files produced by running the apps or the tests
*.db
*.db-shm
*.db-wal
# written by the Claude Code hook
.gate.log
# jscpd output
.jscpd-report/
# experiment runs: keep the committed example and every run's results.csv, not the transcripts and artefacts
experiment/results/*
!experiment/results/example-results.csv
!experiment/results/*/
experiment/results/*/*
!experiment/results/*/results.csv
